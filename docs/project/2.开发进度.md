3.18
开发滑块校验功能，用于注册页面。
当用户点击获取验证码时，向服务器访问http://localhost:8080/api/mall/admin/user/verify/captcha,来获取滑块

3.25
滑块校验功能开发完毕，测试通过。
当校验通过时，前端会访问http://localhost:8080/api/mall/admin/user/verify/smscode,获取验证码

3.27
手机号密码登录功能开发完毕，测试通过
点击登录按钮，会触发滑块校验，来获取Ticket
增加密码错误累计功能，10分钟内只能错3次
登录成功后下发token

4.3
飞书扫码登录开发中
主要是service/token,获取token，通过token换取用户信息

4.6
飞书扫码登录开发完成
手机扫码未实现，需要后端持续监听，还需要公共IP
电脑授权可登录成功

4.8
手机验证码开发中（待优化）
通过飞书发送验证码，已开发完毕
验证码验证待开发中

4.10
手机验证码开发完毕（待优化）
前端优化点：
    1.获取手机验证码、飞书扫码登录，无需再拿appCode=2000 访问接口，后端可直接定义
    2.登录成功，不需要再访问/user/info拿用户信息，因为登录的接口会直接返回用户信息（4.14 已完成）
    3.前端界面优化，做个人中心（4.14 已完成，还待优化）
    4.发送验证码后，弹窗提示，不要用浏览器自带的提示（4.13 已完成）
    5.优化前端登录页，修改样式（4.14 已完成）
后端优化点：
    1.拿取TenantAccessToken需要加锁，因为飞书官方接口有限制，另一方面，在过期时间内不需要再请求 

4.15
    1.权限菜单获取，前端待开发
    2.飞书解绑，扫码绑定，前端待开发
